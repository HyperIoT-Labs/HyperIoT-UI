import { PageInputDirective } from './page-input.directive';

describe('PageInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PageInputDirective({ nativeElement: {} });
    expect(directive).toBeTruthy();
  });
});
